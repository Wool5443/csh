#include <stdlib.h>
#include <stdio.h>

int main(int argc, const char* argv[])
{
    if (argc != 2)
    {
        printf("Usage: %s <path to script file>\n", argv[0]);
        return EXIT_FAILURE;
    }



    return 0;
}
